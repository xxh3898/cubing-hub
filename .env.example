# Local development secrets
# Copy to `.env` and replace placeholder values before running local services.

LOCAL_DB_PASSWORD=change-me
LOCAL_JWT_SECRET=base64-encoded-secret
LOCAL_GRAFANA_ADMIN_PASSWORD=change-me
LOCAL_FEEDBACK_DISCORD_WEBHOOK_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_AUTH=true
SMTP_STARTTLS_ENABLE=true
SMTP_FROM_ADDRESS=
